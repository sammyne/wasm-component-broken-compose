package docs:calculator@0.1.0;

interface hi {
  use docs:adder/types@0.1.0.{balance};

  variant wallet {
      btc(balance),
      eth(balance),
  }

  greet: func(a: wallet);
}

world xml-hello {
  export hi;

  import docs:adder/api@0.1.0;
  import docs:adder/types@0.1.0;
}
